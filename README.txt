FENLAND ANGELS RADIO - PRESENTER DASHBOARD

Upload this entire folder to your existing GitHub repository as:

/presenter/

The page will then be available at:
https://fenlandangelsradio.github.io/FENLAND-ANGELS-RADIO/presenter/

FILES
- index.html        Dashboard page
- style.css         Dashboard styling
- app.js            Clock and live-data reader
- presenter-data.json  Demo/sample data source

IMPORTANT
This version is READ ONLY and contains no PlayIt control buttons.

At the moment presenter-data.json contains demo data so you can test the page on GitHub immediately.

NEXT STAGE
We will connect PlayIt Live to this dashboard by replacing/updating presenter-data.json through a small live-data bridge. Do NOT make GitHub commits every few seconds. The dashboard is already prepared to poll its data source every 5 seconds.

SECURITY
A normal GitHub Pages URL is public. Do not use this for real presenter information until access control is added. We can add the access-control layer after the dashboard itself is working.
